package com.taskmanager.profile;

/** Kinds of account-level events recorded in a user's activity history. */
public enum UserActivityType {
    ACCOUNT_CREATED,
    EMAIL_VERIFIED,
    PASSWORD_CHANGED,
    PROFILE_UPDATED,
    AVATAR_UPDATED,
    AVATAR_REMOVED,
    NOTIFICATION_PREFERENCES_UPDATED
}
